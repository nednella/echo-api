spring:
    sql:
        init:
            mode: always

cors:
    allowed-origins: '*' # comma-separated values

logging:
    level:
        org:
            springframework:
                web: DEBUG
                jpa:
                    core: TRACE
